{
  "name": "server-weather-api",
  "version": "0.0.1",
  "description": "Recuperating weather then sending it to client",
  "dependencies": {
    "express": "^4.18.1",
    "request": "^2.88.2",
    "socket.io": "^4.5.1"
  }
}
